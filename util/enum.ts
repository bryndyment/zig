export enum Modes {
  UNKNOWN,
  TODAY,
  SHARED
}

export enum Positions {
  BOTTOM_LEFT = 1,
  BOTTOM_RIGHT,
  TOP_LEFT,
  TOP_RIGHT
}

export enum Statuses {
  INITIAL = 1,
  IN_PROGRESS,
  COMPLETE
}
